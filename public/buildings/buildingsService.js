angular.module("ClashApp").factory("BuildingsService", function() {
// angular.module("ClashApp").factory("BuildingsService", function($resourse) {
  // var buildings = $resourse('http://localhost:8000/kingdom/:id/buildings/', {id: 0});

  var buildings = "epulet";
  return buildings;
})
